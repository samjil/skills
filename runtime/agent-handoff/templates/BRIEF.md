# BRIEF — <프로젝트 이름>

이 파일은 **양쪽이 매 요청마다 다시 적지 않기 위한 서 있는 맥락**이다. 요청문에는 그 건에서만
쓰는 내용만 적고, 아래는 이미 아는 것으로 친다.

**채널 규약(폴더 구조 · 번호 · 원장 · 상태 어휘 · 메시지 형식)은 여기 적지 않는다.**
`agent-handoff` 스킬에 있다. 이 파일에는 **이 프로젝트에만 해당하는 것**만 둔다 — agy 가
세션 첫 진입 때 훑는 파일이라 짧을수록 좋다.

agy 는 읽기만 한다. 고쳐야 할 내용이 보이면 `msg/` 에 메시지로 제안한다. 고치는 사람은
Claude 다.

<!-- 
[AI 자율 작성 팁]
1. README.md, docs/, package.json (또는 build.gradle, Cargo.toml 등)을 읽고 핵심만 간추린다.
2. 장황한 코드 복사 금지: 파일 위치와 실행 명령어, 반드시 지켜야 할 금기 사항만 1~2페이지 내로 작성한다.
-->

---

## 1. 무엇을 만들고 있나

<!-- README.md 및 프로젝트 기획/설계 문서 참조 -->
- **저장소 경로**: `<저장소 절대 경로>` (브랜치: `<기본 브랜치명>`)
- **서비스 목적**: <한두 문단. 무엇을 하는 앱/시스템이며 핵심 기능이 무엇인가.>
- **화면 및 주요 구성**: <화면 개수, 주요 라우트, 핵심 모듈 구성>

<!-- 갈래(테넌트, 고객사, 모드 등)가 둘 이상이면 표로 정리 -->
| 갈래 | 식별자 | 특징 |
|---|---|---|
| 기본 | default |  |

---

## 2. 환경

<!-- package.json의 scripts 및 설정 파일, 런타임 버전 참조 -->
- **OS · 셸**: Windows (PowerShell) / macOS (zsh) / Linux (bash)
- **런타임 / 언어**: Node.js vX.X / Python 3.X / TypeScript 5.X 등
- **핵심 스택**: 프레임워크 및 상태관리, 통신 라이브러리
- **필수 실행/검증 명령어**:
  - 개발 서버: `npm run dev` (또는 해당 명령)
  - 검증/타입 검사: `npm run lint` / `npx tsc -b`
  - 프로덕션 빌드: `npm run build`

---

## 3. 정본 규칙

문서에 적힌 것을 그대로 옮긴다. **출처(`파일:줄`)를 함께 둔다.**

- <무엇이 정본인가. 손대면 안 되는 문구, 지우면 안 되는 코드값, 박으면 안 되는 설정.> (`출처`)
- <지어낸 값을 쓰지 않는다 같은, 이 프로젝트에서 실제로 깨졌던 약속.> (`출처`)

**무엇을 믿을지 갈리는 자리는 믿을 쪽과 이유를 함께 적는다.** 이유가 없으면 다음 사람이
되돌린다.

| 어긋나는 것 | 믿을 쪽 | 왜 |
|---|---|---|
| 예: 시안 vs 기획서 | 기획서 | 시안은 레이아웃 참고용 (`docs/planning.md:12`) |

<!-- 앞선 판단을 뒤집은 것이 있으면 "되돌리지 않는다" 라고 못 박고 언제 누가 정했는지 적는다. -->
