# INDEX — 메시지 원장
맨 아래에 **한 줄만 덧붙인다.** 위쪽 줄은 고치지 않는다 — 상태 칸만 예외다.
**표 사이에 빈 줄을 넣지 않는다.** 마지막 줄 바로 다음 행에 붙여야 표로 읽힌다.
상태는 `보냄` · `읽음` · `처리됨` · `확인함` · `보류` 중 하나. `보냄` 으로 방치하지 않는다.
| 번호 | 시각 | 방향 | 제목 | 상태 |
|---|---|---|---|---|
